{
  "name": "WTELE-WEB",
  "version": "1.0.0",
  "description": "Your project description",
  "main": "index.js",
  "scripts": {
    "build": "gulp",                      // Build command for Vercel
    "dev": "gulp watch",                  // Development command to watch changes
    "deploy": "gulp deploy",              // Optional: deploy command, if needed
    "start": "npm run build",             // Vercel will run this on deployment
    "postinstall": "npm run build"        // Ensure build happens after dependencies are installed
  },
  "dependencies": {
    "sw-precache": "^5.0.0",              // Service worker precaching
    "karma": "^6.3.4",                    // Karma testing framework
    "karma-cli": "^2.0.0"                 // CLI tool for Karma
  },
  "devDependencies": {
    "gulp": "^4.0.2",                     // Gulp task runner
    "gulp-load-plugins": "^1.5.0",         // Gulp plugins loader
    "gulp-imagemin": "^7.1.0",             // Image minification
    "gulp-minify-html": "^1.0.4",          // HTML minification
    "gulp-standard": "^1.4.0",             // JavaScript linting
    "gulp-replace": "^1.1.2",              // Find & replace strings in files
    "gulp-uglify": "^3.0.2",               // JavaScript minification
    "gulp-concat": "^2.6.1",               // Concatenate files
    "gulp-ng-annotate": "^2.0.1",          // AngularJS annotation for minification
    "gulp-less": "^4.0.0",                 // LESS CSS preprocessor
    "gulp-zip": "^5.1.0"                  // Create ZIP files (for releases)
  },
  "engines": {
    "node": ">=14.0.0",                   // Ensure using compatible Node.js version
    "npm": ">=6.0.0"
  },
  "author": "Your Name",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Aalee5699/WTELE-WEB.git"
  },
  "bugs": {
    "url": "https://github.com/Aalee5699/WTELE-WEB/issues"
  },
  "homepage": "https://github.com/Aalee5699/WTELE-WEB#readme"
}
